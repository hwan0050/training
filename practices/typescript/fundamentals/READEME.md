# TypeScript 기초 학습

## 📅 학습 날짜
2024-11-18

## 📚 학습 내용

### 1. 기본 타입 (basic-types.ts)
- **Primitive Types**: string, number, boolean
- **Array**: number[], Array<string>
- **Tuple**: [string, number] - 고정된 길이와 타입의 배열
- **Enum**: 열거형 타입으로 상수 그룹 정의
- **Any vs Unknown**: Any는 타입 체크 우회, Unknown은 타입 체크 필요
- **Void**: 반환값 없는 함수
- **Never**: 절대 반환하지 않는 함수 (무한 루프, 예외 발생)

### 2. 인터페이스 (interfaces.ts)
- **기본 인터페이스**: 객체의 구조 정의
- **Optional 속성**: `?` 사용으로 선택적 속성
- **Readonly 속성**: 읽기 전용, 수정 불가
- **함수 타입 인터페이스**: 함수 시그니처 정의
- **인터페이스 확장**: `extends`로 상속
- **인덱스 시그니처**: 동적 속성 정의
- **클래스 구현**: `implements`로 인터페이스 구현

### 3. 제네릭 (generics.ts)
- **제네릭 함수**: `<T>` 타입 파라미터로 재사용 가능한 함수
- **제네릭 배열**: 타입 안전한 배열 처리
- **제네릭 인터페이스**: 유연한 타입 정의
- **제네릭 클래스**: 타입 안전한 데이터 저장소
- **제네릭 제약조건**: `extends`로 타입 제한
- **keyof 연산자**: 객체 키의 타입 추출
- **유틸리티 타입**: Partial, Pick, Omit 등 내장 타입

## 💻 실습 파일
```
practices/typescript/fundamentals/
├── package.json           # npm 프로젝트 설정
├── package-lock.json      # 의존성 잠금 파일
├── tsconfig.json          # TypeScript 컴파일 설정
├── node_modules/          # 설치된 패키지
├── basic-types.ts         # 기본 타입 학습
├── basic-types.js         # 컴파일된 JavaScript
├── interfaces.ts          # 인터페이스 학습
├── interfaces.js          # 컴파일된 JavaScript
├── generics.ts            # 제네릭 학습
├── generics.js            # 컴파일된 JavaScript
└── README.md              # 학습 노트 (이 파일)
```

## 🔧 환경 설정
```bash
# 프로젝트 초기화
npm init -y

# TypeScript 설치
npm install --save-dev typescript @types/node

# tsconfig.json 생성
npx tsc --init

# TypeScript 컴파일
npx tsc 파일명.ts

# JavaScript 실행
node 파일명.js
```

## 📖 주요 개념

### 타입 안전성
TypeScript의 가장 큰 장점은 **컴파일 타임 타입 체크**
- 런타임 오류를 컴파일 타임에 미리 발견
- IDE 자동완성 강력해짐
- 리팩토링이 안전해짐

### 인터페이스 vs 타입 별칭
```typescript
// 인터페이스 (확장 가능)
interface User {
    name: string;
}
interface Admin extends User {
    role: string;
}

// 타입 별칭 (유니온 등에 유리)
type ID = string | number;
```

### 제네릭의 장점
- 타입 재사용성 증가
- 타입 안전성 유지
- 코드 중복 감소

## ✅ 실습 완료
- [x] TypeScript 환경 설정 완료
- [x] 기본 타입 학습 및 실습
- [x] 인터페이스 정의 및 사용
- [x] 제네릭 개념 이해 및 적용
- [x] 타입 안전성 체험
- [x] 유틸리티 타입 활용
- [x] 코드 컴파일 및 실행 확인

## 💡 배운 점
- TypeScript는 JavaScript의 슈퍼셋으로 타입 시스템 추가
- 타입 체크로 버그를 사전에 방지할 수 있음
- 인터페이스로 명확한 계약(Contract) 정의 가능
- 제네릭으로 재사용 가능하면서 타입 안전한 코드 작성
- Any 타입은 TypeScript의 장점을 무효화하므로 피해야 함
- Unknown 타입은 타입 가드와 함께 사용하면 안전

## 🚨 주의사항
- `any` 타입은 가능하면 사용하지 않기
- `unknown` 사용 시 반드시 타입 체크 후 사용
- `strict: true` 옵션으로 엄격한 타입 체크 권장
- null/undefined 체크를 꼼꼼히 하기
- 타입 단언(`as`)보다는 타입 가드 사용

## 🎯 다음 학습 목표
- React + TypeScript 조합 학습
- 고급 타입 (Union, Intersection, Conditional Types)
- 타입 가드와 타입 단언 심화
- Decorator 학습
- TypeScript 컴파일 옵션 최적화

## 🔗 참고 자료
- [TypeScript 공식 문서](https://www.typescriptlang.org/docs/)
- [TypeScript 핸드북](https://www.typescriptlang.org/docs/handbook/intro.html)
- [TypeScript Deep Dive](https://basarat.gitbook.io/typescript/)
- [Learning Path](../../../docs/LEARNING_PATH.md)

## 📊 학습 시간
- 환경 설정: 30분
- 기본 타입 학습: 1시간
- 인터페이스 학습: 1시간
- 제네릭 학습: 1.5시간
- **총 학습 시간: 약 4시간**